# AI layer (required for AI-designed layouts; without it the server falls back
# to a deterministic heuristic planner). Pick one or more providers; the
# default provider is selected via LLM_PROVIDER (or per-request `llmProvider`).
ANTHROPIC_API_KEY=sk-ant-...
# Optional: override the model used by the UI planner
# ANTHROPIC_MODEL=claude-haiku-4-5-20251001

# Optional additional LLM providers
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o
# GEMINI_API_KEY=...
# GEMINI_MODEL=gemini-2.0-flash
# LLM_PROVIDER=anthropic

# Design system selected at setup (glass | shadcn | material | plain). Can also
# be switched at runtime via POST /api/design-systems/active
MCP_DESIGN_SYSTEM=glass

# Allow fetching localhost/private-network endpoints (off by default — SSRF guard)
# MCP_ALLOW_PRIVATE_ENDPOINTS=1

# Endpoint fetch limits
# MCP_MAX_FETCH_BYTES=2097152
# MCP_FETCH_TIMEOUT_MS=15000
