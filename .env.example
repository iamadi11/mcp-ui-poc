# AI layer (required for AI-designed layouts; without it the server falls back
# to a deterministic heuristic planner)
ANTHROPIC_API_KEY=sk-ant-...
# Optional: override the model used by the UI planner
# ANTHROPIC_MODEL=claude-opus-4-8

# Design system selected at setup (shadcn | material | plain). Can also be
# switched at runtime via POST /api/design-systems/active
MCP_DESIGN_SYSTEM=shadcn

# Allow fetching localhost/private-network endpoints (off by default — SSRF guard)
# MCP_ALLOW_PRIVATE_ENDPOINTS=1

# Endpoint fetch limits
# MCP_MAX_FETCH_BYTES=2097152
# MCP_FETCH_TIMEOUT_MS=15000
