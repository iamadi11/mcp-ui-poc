# Menu bar visual pass

Teal productivity chrome (`AgentChrome`), not a purple AI default. SwiftUI menu-bar search in ui-ux-pro-max returned no stack rows; status feedback and badge guidance applied.

## What changed

- Mark and wordmark in the header
- Status card keeps a text phase badge and adds a leading accent so state is not color-only
- Status card has one combined accessibility label (phase, heard, result)
- **Act once** sits in its own well and fills when armed
- 8/12/16 spacing, 10pt continuous corners, system surfaces so light and dark both separate card from window

## Unchanged behavior

Listen, Act once, Battery, Ollama, AX journey, and Permissions still do the same work.
