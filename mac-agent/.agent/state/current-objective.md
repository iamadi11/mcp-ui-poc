# Current objective

Listen records the microphone and transcribes that audio, instead of the streaming path that returned an empty line.

The panel shows one 22pt sentence. Installed at `~/Applications/Mac Agent.app`.

**Held for you:** live click/type still has to run inside this app (Act once, then AX journey). The CLI cannot use the app's Accessibility grant.
