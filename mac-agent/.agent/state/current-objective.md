# Current objective

Voice remote is installed at `~/Applications/Mac Agent.app`.

The panel is 320pt: one status word, the transcript, then the next action (dry-run or live). Listen becomes Stop while listening. Battery, Ollama, and AX sit under Tools. Permissions stay collapsed.

**Held for you:** live click/type still has to run inside this app (Act once, then AX journey). The CLI cannot use the app's Accessibility grant.
