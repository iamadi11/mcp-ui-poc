# macOS app shell — open on a Mac with Xcode. Not built in Linux CI.

Placeholder for SwiftUI menu-bar UI (Phase 10 packaging).

The security-critical runtime lives in the Swift package at repo root (`MacAgentCore`, `MacAgentSecurity`, …).
